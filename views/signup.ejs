<%- include('partials/header.ejs') %>

  <div class="d-lg-flex half">
    <!-- intro -->
    <%- include('partials/intro.ejs') %>


    <!-- forms -->
    <div class="contents order-2 order-md-1">
  
      <div class="container">
        <div class="row align-items-center justify-content-center" id="sign-up-form">
          <div class="col-md-7">
            <h3>Sign <strong>Up</strong></h3>
            <% if (locals.messages.errors) { %>
              <% messages.errors.forEach( el => { %>
                  <div class="alert alert-danger"><%= el.msg %></div>
              <% }) %>    
            <% } %>
      
            <form class="flex-centered" action="/signup" method="POST">
                <input name="userName" class="form-control" type="username" name="userName" placeholder="Name" id="username">
                <input name="email" class="form-control" type="email" name="email" placeholder="Email" id="email">
                <input name="password" type="password" name="password" class="form-control" placeholder="Password" id="password">
                <input name="confirmPassword" type="password" name="password" class="form-control" placeholder="Confirm Password" id="password">
                
                <input class="btn btn-block btn-primary" type="submit" value="Submit">
                <a id="login-signup" href="/login">Log In</a>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>  
  
<%- include('partials/footer.ejs') %>
